import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary (same as web #16a34a)
  static const primary = Color(0xFF16a34a);
  static const primaryHover = Color(0xFF15803d);
  static const primaryDark = Color(0xFF14532d);
  static const primaryMid = Color(0xFF166534);
  static const primaryLight = Color(0xFFdcfce7);
  static const primaryLighter = Color(0xFFf0fdf4);

  // Accent
  static const accent = Color(0xFF10b981);
  static const accentLight = Color(0xFF6ee7b7);

  // Backgrounds
  static const surface = Color(0xFFf7faf8);
  static const white = Color(0xFFFFFFFF);

  // Borders
  static const border = Color(0xFFd1fae5);
  static const borderMid = Color(0xFFa7f3d0);
  static const borderNeutral = Color(0xFFe4e7ec);

  // Text
  static const ink = Color(0xFF0f1f12);
  static const inkBody = Color(0xFF1e3a22);
  static const muted = Color(0xFF4b7a54);
  static const subtle = Color(0xFF86a98d);

  // Semantic
  static const successBg = Color(0xFFdcfce7);
  static const error = Color(0xFFdc2626);
  static const errorBg = Color(0xFFfef2f2);
  static const warning = Color(0xFFf59e0b);
  static const warningBg = Color(0xFFfffbeb);
}
